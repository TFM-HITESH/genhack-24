import React from 'react';

type Props = {};

export default function TeamNRules(props: Props) {
    return (
        <div className="w-full rounded-3xl flex flex-col items-center justify-center bg-orange-400 border-white border-2">
            teamNRules : AKSHAY
            <p>Add some more info. Team size = 4 (fixed)</p>
            <p>
                Rules should just be a redirect now. Will fill with pdf or
                something like that later
            </p>
            <p>
                Refer to - https://ui.aceternity.com/components/animated-tooltip
            </p>
        </div>
    );
}
